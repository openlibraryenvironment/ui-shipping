import React from 'react';
import ResourceSharing from '@folio/rs';

export default (props) => (
  <span>
    <h1>This is the shipping app.</h1>
    <p>Ship loaned items to borrowing library</p>
  </span>
);
